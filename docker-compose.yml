version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: password_manager_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: supersecretpassword
      POSTGRES_DB: PasswordManagerVault
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: